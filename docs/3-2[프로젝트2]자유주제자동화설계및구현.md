# 📌 프로젝트 2. YouTube RSS 자동 수집 및 Discord 알림 자동화

## 프로젝트 개요

YouTube RSS Feed를 활용하여 새로운 영상이 업로드되면 자동으로 감지하고,

- Notion 데이터베이스에 저장
- Discord 전용 채널에 알림

까지 자동으로 수행되는 워크플로우를 구축하였다.

---

# 사용 기술

- n8n
- YouTube RSS Feed
- Notion API
- Discord Bot API

---

# 전체 워크플로우

```
RSS Feed Trigger
        │
        ▼
     IF (제목 필터)
        │
        ▼
Get Many Database Pages
        │
        ▼
 IF (중복 여부 확인)
        │
   ┌────┴────┐
   │         │
중복O      중복X
 종료         │
              ▼
      Create Database Page
              │
              ▼
      Discord Message
```

> 📷 <img width="50%" alt="image" src="https://github.com/user-attachments/assets/4aa5b8c2-8ef5-45ef-b151-ccb81c7e52f0" />


---

# 1. RSS Feed Trigger

YouTube 채널의 RSS Feed를 등록하여 새로운 영상이 업로드될 경우 자동으로 실행되도록 설정하였다.

RSS에서 전달받는 데이터

- 제목(title)
- 링크(link)
- 업로드일(pubDate)
- 작성자(author)
- 영상 ID(id)

<img width="50%" alt="image" src="https://github.com/user-attachments/assets/263c9e10-8e97-4bca-b885-a19fbbf5dbcc" />


---

# 2. 첫 번째 IF 노드

모든 영상을 저장하지 않고 특정 키워드만 저장하도록 조건을 설정하였다.

예시)

```
제목에 "전기차" 포함
```

조건을 만족하는 영상만 다음 단계로 이동한다.

> 📷 IF 노드 설정 화면

---

# 3. Notion 중복 검사

Notion Database의 기존 데이터를 조회한다.

사용 노드

```
Get Many Database Pages
```

Database

```
YouTube Trending
```

중복 여부 확인을 위해 Database에 저장된 **id** 값을 조회한다.

> 📷 Get Many Database Pages 설정 화면

---

# 4. 두 번째 IF 노드 (중복 체크)

RSS에서 전달받은 영상 ID와

Notion Database에 저장되어 있는 ID를 비교하였다.

비교식

```
{{ $json.id }}
```

이미 존재하는 영상이면

```
True
→ 종료
```

존재하지 않는 경우

```
False
→ Create Database Page
```

로 이동하도록 구성하였다.

> 📷 IF2 설정 화면

---

# 5. Create Database Page

중복되지 않은 영상만 Notion Database에 자동 저장하였다.

저장 항목

| Property | 값 |
|----------|----|
| Title | RSS title |
| Link | RSS link |

(업로드일은 이번 과제에서는 제외)

사용한 Expression

Title

```javascript
{{ $('RSS Feed Trigger').item.json.title }}
```

URL

```javascript
{{ $('RSS Feed Trigger').item.json.link }}
```

> 📷 Create Database Page 설정 화면

---

# 6. Discord 알림

새로운 영상이 등록되면 Discord Bot을 이용하여 전용 채널에 메시지를 전송하였다.

메시지 형식

```
🔥 새로운 YouTube 영상이 등록되었습니다!

제목
전기차 타고 있다면 이 문자, 그냥 넘기지 마세요!

링크
https://youtube.com/...

업로드
2026-07-08T09:05:14Z
```

사용한 Expression

제목

```javascript
{{ $('RSS Feed Trigger').item.json.title }}
```

링크

```javascript
{{ $('RSS Feed Trigger').item.json.link }}
```

업로드일

```javascript
{{ $('RSS Feed Trigger').item.json.pubDate }}
```

> 📷 Discord 노드 설정 화면

> 📷 Discord 수신 결과 화면

---

# 최종 결과

자동화 흐름

```
YouTube 영상 업로드

↓

RSS Feed 감지

↓

키워드 필터

↓

Notion 중복 검사

↓

중복 아니면 저장

↓

Discord 알림
```

모든 과정이 자동으로 실행되는 것을 확인하였다.

---

# 구현 결과

✅ RSS 자동 감지

✅ 특정 키워드 필터링

✅ Notion 중복 검사

✅ 중복 데이터 저장 방지

✅ Notion 자동 저장

✅ Discord 자동 알림

---

# 느낀 점

이번 과제를 통해 n8n의 IF 노드를 이용한 조건 분기와 Notion Database 조회 기능을 활용하여 중복 데이터를 방지하는 방법을 익힐 수 있었다.

또한 Discord Bot과 연동하여 실시간 알림 시스템을 구축함으로써 실제 업무에서도 활용 가능한 자동화 워크플로우를 구현할 수 있었다.
